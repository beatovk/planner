# Requirements for Step 8: FTS5 + Redis Cache + FastAPI
# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Redis client
redis>=5.0.0

# SQLite (built-in, but ensuring compatibility)
# No additional package needed for FTS5

# Additional utilities
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Logging and monitoring
structlog>=23.2.0

# Data handling
orjson>=3.9.0
